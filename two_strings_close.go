package main

import "fmt"

func main() {
	word1 := "abc"
	word2 := "bca"
	areClose := closeStrings(word1, word2)
	fmt.Println(areClose)
}

func closeStrings(word1 string, word2 string) bool {
    var operation1 bool
    var operation2 bool
	
    
}